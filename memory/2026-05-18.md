# 2026-05-18 工作日志（更新至 03:39）

## ⚠️ 当前阻塞
- **出题端点返回 HTML 500**：`POST /api/quizzes/generate` 报 `Internal Server Error`（HTML），前端收到 `is not valid JSON`
- `GET /api/quizzes/test-connection/` ✅ 正常（DeepSeek Flash 连通）
- 网络问题已解决：原来是 Windows 代理环境变量拦截，`$env:HTTP_PROXY=""` 后 `curl` 通了
- 已部署全局 JSON 异常处理器（commit `f7e3620`），**下次启动会打印完整 traceback**

## 下次继续步骤
1. `cd D:/Projects/study-os && git pull`
2. 后端：`D:\Projects\study-os\backend\venv\Scripts\python.exe -m uvicorn src.main:app --reload --port 8000`
3. 浏览器 `http://localhost:3000/quiz` → 生成题目
4. **同时观察**：浏览器弹窗 + 后端终端 traceback → 截图发我

## 本周完成
1. GitHub 仓库搭建（论文库 + 学舟）
2. StudyOS 全链路免费化（移除 OpenAI，月费 ¥30-50 → ¥1-2）
3. LLM 双模型分级（Pro/Flash）
4. 学舟首页暗色赛博UI
5. AI 出题 + 批改引擎（6表模型 + API）
6. 文件上传解析（PDF/TXT/MD）
7. Windows 环境搭建（Python 3.12 venv, VS Build Tools）
8. 全局异常处理 + 连接测试端
